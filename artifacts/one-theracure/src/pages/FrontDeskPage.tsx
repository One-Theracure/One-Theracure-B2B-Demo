import FrontDeskLayout from "@/components/frontdesk/FrontDeskLayout";

/** /frontdesk route. Thin wrapper around the existing FrontDeskLayout. */
const FrontDeskPage = () => <FrontDeskLayout />;

export default FrontDeskPage;
